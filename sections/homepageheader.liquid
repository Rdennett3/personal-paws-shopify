{{ 'homepageheader.css' | asset_url | stylesheet_tag }}

{%- if section.settings.enable_quick_add -%}
    <link rel="stylesheet" href="{{ 'quick-add.css' | asset_url }}" media="print" onload="this.media='all'">
    <script src="{{ 'quick-add.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
  {%- endif -%}

  <section class="homeheaderouter">
    <div class="homeheaderitem">
        <h1>The School of Personal Paws</h1>
    </div>
    <div class="homeheaderitem">
        <img src="https://www.personal-paws.com/wp-content/uploads/2021/12/hpimg-small.png">
    </div>
  </section>

  {%schema%}
  {
    "name": "Homepage Header",
    "tag": "section"
  }
  {% endschema %}